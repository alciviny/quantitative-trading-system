# Energy Engine

Estrutura modular para estudos e pipelines relacionados a energy.

- Adicione scripts em `features`, `filters`, `models`, `utils` conforme necessidade.
- Pipelines principais devem ficar em `pipeline`.
- Testes em `tests`.

Pronto para expansão futura!